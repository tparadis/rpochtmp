<p id="notice"><%= notice %></p>

<h1>Blacklist</h1>
<h5>Voici la liste des commerces qui sont blacklistés du fichier XSLX. S'ils y apparaissent, ils seront automatiquement ignorés lors de la mise à jour</h5><br/><br/>

<table class="table tabled-bordered tabl-hover table-striped" > 
  <thead>
    <tr>
      <th>Siret</th>
      <th>Enseigne</th>
      <th>Rasoc</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blacklists.each do |blacklist| %>
      <tr>
        <td><%= blacklist.siret %></td>
        <td><%= blacklist.enseigne %></td>
        <td><%= blacklist.rasoc %></td>
        <td><%= link_to 'Show', blacklist %></td>
        <td><%= link_to 'Edit', edit_blacklist_path(blacklist) %></td>
        <td><%= button_to 'Destroy', blacklist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Blacklist', new_blacklist_path %>
