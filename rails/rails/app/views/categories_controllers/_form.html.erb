<%= form_for(@categories_controller) do |f| %>
  <% if @categories_controller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categories_controller.errors.count, "error") %> prohibited this categories_controller from being saved:</h2>

      <ul>
      <% @categories_controller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
